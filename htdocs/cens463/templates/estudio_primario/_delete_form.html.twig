<form method="post" action="{{ path('app_estudio_primario_delete', {'id': estudio_primario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estudio_primario.id) }}">
    <button class="btn">Delete</button>
</form>
