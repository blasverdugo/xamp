{% extends 'base.html.twig' %}

{% block title %}EstudioPrimario{% endblock %}

{% block body %}
    <h1>EstudioPrimario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estudio_primario.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ estudio_primario.numero }}</td>
            </tr>
            <tr>
                <th>Localidad</th>
                <td>{{ estudio_primario.localidad }}</td>
            </tr>
            <tr>
                <th>AnioFinalizado</th>
                <td>{{ estudio_primario.anioFinalizado }}</td>
            </tr>
            <tr>
                <th>Egreso</th>
                <td>{{ estudio_primario.egreso }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_estudio_primario_index') }}">back to list</a>

    <a href="{{ path('app_estudio_primario_edit', {'id': estudio_primario.id}) }}">edit</a>

    {{ include('estudio_primario/_delete_form.html.twig') }}
{% endblock %}
