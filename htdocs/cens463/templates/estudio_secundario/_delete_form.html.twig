<form method="post" action="{{ path('app_estudio_secundario_delete', {'id': estudio_secundario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estudio_secundario.id) }}">
    <button class="btn">Delete</button>
</form>
