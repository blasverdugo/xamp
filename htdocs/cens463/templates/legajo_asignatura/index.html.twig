{% extends 'base.html.twig' %}

{% block title %}LegajoAsignatura index{% endblock %}

{% block body %}
    <h1>LegajoAsignatura index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for legajo_asignatura in legajo_asignaturas %}
            <tr>
                <td>{{ legajo_asignatura.id }}</td>
                <td>
                    <a href="{{ path('app_legajo_asignatura_show', {'id': legajo_asignatura.id}) }}">show</a>
                    <a href="{{ path('app_legajo_asignatura_edit', {'id': legajo_asignatura.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_legajo_asignatura_new') }}">Create new</a>
{% endblock %}
