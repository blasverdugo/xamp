<form method="post" action="{{ path('app_legajo_asignatura_delete', {'id': legajo_asignatura.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legajo_asignatura.id) }}">
    <button class="btn">Delete</button>
</form>
