{% extends 'base.html.twig' %}

{% block title %}Orientada{% endblock %}

{% block body %}
    <h1>Orientada</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ orientada.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ orientada.fecha ? orientada.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_orientada_index') }}">back to list</a>

    <a href="{{ path('app_orientada_edit', {'id': orientada.id}) }}">edit</a>

    {{ include('orientada/_delete_form.html.twig') }}
{% endblock %}
